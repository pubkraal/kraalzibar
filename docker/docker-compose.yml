services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: kraalzibar
      POSTGRES_USER: kraalzibar
      POSTGRES_PASSWORD: kraalzibar
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
